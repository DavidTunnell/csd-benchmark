# Selenium for CSD and AWS Console runners.
selenium>=4.20.0

# selenium-wire would give us HTTP request count + bytes for free, but its
# bundled mitmproxy is not compatible with Python 3.14 (broken on
# blinker._saferef and pkg_resources). v1 of the harness drops it; we pick
# up HTTP metrics via Chrome DevTools Protocol once a v2 of network capture
# is wired. Tracking this in csd_runner.py TODOs.
# selenium-wire>=5.1.0

# webdriver-manager auto-fetches chromedriver matching the local Chrome.
webdriver-manager>=4.0.1

# AWS CLI runner uses boto3 only for setup/cleanup; the runs themselves shell
# out to `aws s3` so we measure the same CLI a customer would use.
boto3>=1.34.0
botocore>=1.34.0

# YAML config (matches csd-benchmark seeders).
PyYAML>=6.0.1

# CSV/results helpers.
# Standard library is fine; nothing extra needed here.
